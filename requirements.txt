# ===========================================================
# Text-to-SQL Engine — Deployment Dependencies
# ===========================================================
# Minimal set required to run the production inference server
# (FastAPI + Gradio + Model). For training dependencies, use:
#   pip install -r requirements-train.txt
# ===========================================================

torch>=2.0.0
transformers>=4.36.0
accelerate>=0.25.0
safetensors>=0.4.0
kagglehub

# Server
uvicorn
fastapi
slowapi>=0.1.9
requests

# Database
sqlalchemy
pymysql
psycopg2-binary

# UI
gradio
pandas